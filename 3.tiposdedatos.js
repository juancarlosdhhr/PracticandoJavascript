

let frase = 'Como dijo un sabio: "A programar se aprende programando" ' ;

let edad = 35;

let booleano = false; /* Es una traduccion al castellano de lo que significaba antes 0 o 1 */

let list = ["pimiento", "seitan", "tomate", "macarrones"] /* Esto es un ARRAY , es decir un listado de cosas
 *//*  Los arrays se empiezan a contabilizar desde 0,1,2,3...Es decir la primera posicion es el 0 */
/* Si lo pongo entre comillas es una frase o String

Si pongo un numero sin comillas entonces es un numero y se puede operar con el. */

/* ARRAY DE OBJETOS */

const pelota = {
color = "azul"
material: "plastico"
precio: 8.5,
resistente: true

}

const nulo = null;  /*  Existe pero no está */
const nodefinido = undefined; /*  No existe */

